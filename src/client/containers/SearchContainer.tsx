import React from 'react';

import {
	connect,
} from 'react-redux';

class SearchComponent extends React.Component {
	public render() {
		return null;
	}
}

export const SearchContainer = connect()(SearchComponent);
